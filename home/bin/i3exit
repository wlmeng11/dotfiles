#!/bin/sh
lock() {
	i3lock
}

case "$1" in
	lock)
		xset dpms force off
		;;
	logout)
		i3-msg exit
		;;
	suspend)
		gksudo -g pm-suspend
		;;
	hibernate)
		;;
	reboot)
		gksudo -g reboot
		;;
	shutdown)
		gksudo -g shutdown now
		;;
	*)
		echo "Usage: $0 {lock|logout|suspend|hibernate|reboot|shutdown}"
		exit 2
esac

exit 0
