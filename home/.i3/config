# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Use Alt key as mod
set $mod Mod1

exec --no-startup-id ~/bin/mouse.sh
exec --no-startup-id ~/bin/keymap.sh
exec --no-startup-id ~/bin/display.sh

##########

# Startup programs
exec --no-startup-id compton --backend glx --sw-opti --vsync opengl-swc --paint-on-overlay || xcompmgr # Compositing
exec --no-startup-id xfce4-volumed # volume control
exec --no-startup-id nm-applet # Network Manager
exec --no-startup-id synapse # program launcher
exec --no-startup-id gnome-screensaver
exec --no-startup-id clipit # clipboard manager
exec --no-startup-id gpg-agent --daemon --enable-ssh-support --write-env-file ~/.gpg-agent-info
exec --no-startup-id gtk-redshift -l 37.44:-122.14 -t 6500:4700
exec --no-startup-id pidgin
exec --no-startup-id /usr/lib/eclipse/eclimd

exec --no-startup-id pactl set-sink-volume 0 '20%'
exec --no-startup-id xbacklight -set 50

for_window [class="[.]*"] floating enable;

for_window [class="Firefox" instance="Navigator"] floating disable;
for_window [class="Iceweasel" instance="Navigator"] floating disable;
for_window [class="Chromium" ] floating disable;
for_window [class="URxvt"] floating disable;
for_window [class="Gvim"] floating disable;
for_window [class="Vi"] floating disable; # for gvim sessions started in terminal then detached
for_window [class="Thunderbird" instance="Mail"] floating disable;
for_window [class="Icedove" instance="Mail"] floating disable;
for_window [class="Evince"] floating disable;
for_window [title="wlmeng - File Manager"] floating disable; # thunar default main window
for_window [class="Emacs" ] floating disable;
for_window [title="<Student Version> MATLAB R2013a"] floating disable;
for_window [class="VirtualBox" ] floating enable;
for_window [instance="sun-awt-X11-XFramePeer" ] floating disable;
for_window [class="libreoffice-writer"] floating disable;
for_window [instance="VCLSalFrame"] floating disable;
for_window [class="Wine" instance="SketchUp.exe" ] floating disable;

for_window [class="Icedove" instance="Mailnews"] floating enable;
for_window [window_role="pop-up"] floating enable
for_window [window_role="utility"] floating enable
for_window [window_role="toolbar"] floating enable
for_window [window_role="splash"] floating enable
for_window [title="VIM - ATTENTION"] floating enable
for_window [title="Print"] floating enable;

# Floating borderless windows
for_window [class="Xfce4-notifyd"] floating enable; border none; focus mode_toggle
for_window [class="Synapse"] floating enable; border none
for_window [class="Steam"] floating enable; border none

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# take screenshot with PrintScreen
bindsym Print exec scrot '%Y-%m-%d-%T_$wx$h.png' -e 'mv $f ~/Pictures/screenshots/'
bindsym $mod+Print exec scrot '%Y-%m-%d-%T_$wx$h.png' -e 'mv $f ~/Pictures/screenshots/' -s # select region with mouse

bindcode 202 exec i3lock
bindsym XF86Lock exec i3lock

# check external display fith Fn+<F7>
bindcode 235 exec ~/bin/xdisplay.sh

# toggle touchpad with Fn+<F8>
bindcode 199 exec ~/bin/touchpadToggle.sh

# lock button
# bindcode 160 exec i3lock
bindcode 160 exec gnome-screensaver-command --lock

# bind volume buttons
# Alternatively, use xfce4-volumed
# bindcode 121 # mute
# bindcode 122 # volume down
# bindcode 123 # volume up

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
bindsym Mod1+Ctrl+t exec i3-sensible-terminal

# other keybindings to applications
bindsym Mod1+Ctrl+f exec thunar
bindsym Mod4+f exec firefox
bindsym Mod4+t exec thunderbird

# kill focused window
bindsym $mod+Shift+q kill
bindsym Mod1+F4 kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

bindsym Mod1+Ctrl+Up exec xrandr -o normal
bindsym Mod1+Ctrl+Right exec xrandr -o right
bindsym Mod1+Ctrl+Left exec xrandr -o left
bindsym Mod1+Ctrl+Down exec xrandr -o inverted

# change focus
# VIM style:
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
# VIM style:
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
# bindsym $mod+h split h
bindsym $mod+semicolon split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+Shift+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+p focus parent

# focus the child container
bindsym $mod+c focus child

bindsym Mod4+Tab workspace back_and_forth

# rename workspace
bindsym $mod+comma exec i3-input -F 'rename workspace to %s' -P 'New name: '

# Assign programs to specific workspaces
assign [class="Pidgin"] 10:chat
assign [class="Thunderbird"] 9:mail
assign [class="Icedove"] 9:mail

# switch to workspace
bindsym $mod+1 workspace 1:web
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8:music
bindsym $mod+9 workspace 9:mail
bindsym $mod+0 workspace 10:chat

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1:web
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8:music
bindsym $mod+Shift+9 move container to workspace 9:mail
bindsym $mod+Shift+0 move container to workspace 10:chat

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window's width.
        # Pressing right will grow the window's width.
        # Pressing up will shrink the window's height.
        # Pressing down will grow the window's height.
        # VIM style:
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"
bindsym Mod4+Up exec i3-msg move workspace to output up
bindsym Mod4+Down exec i3-msg move workspace to output down
bindsym Mod4+Left exec i3-msg move workspace to output left
bindsym Mod4+Right exec i3-msg move workspace to output right

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

bar {
	output VGA1
	#status_command i3status --config ~/.i3/i3status.conf
	status_command i3status
	tray_output primary
}
bar {
	output DP1
	#status_command i3status --config ~/.i3/i3status.conf
	status_command i3status
	tray_output primary
}
bar {
	output VBOX0
	status_command i3status
	tray_output primary
}
bar {
	output LVDS1
	mode hide
	modifier $mod
	# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
	# font pango:DejaVu Sans Mono 10
	#status_command i3status --config ~/.i3/i3status.conf
	status_command i3status
}
